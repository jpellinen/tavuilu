# URL of the Fastify API (proxied by Vite dev server in development)
VITE_API_URL=http://localhost:3000

# Base URL for serving word images; flip to a CDN URL in production
VITE_IMAGE_BASE_URL=http://localhost:3000
